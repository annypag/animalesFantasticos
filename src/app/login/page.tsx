import { LoginScreen } from "@/features/login/pages/login-screen";

// Route entrypoint for "/login". UI and state live in features/login.
export default function LoginPage() {
  return <LoginScreen />;
}
